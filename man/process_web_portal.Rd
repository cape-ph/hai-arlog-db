% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ExportCsv.R
\name{process_web_portal}
\alias{process_web_portal}
\title{Process Web Portal Data}
\usage{
process_web_portal(df, CSV_OUTPUT_DIR)
}
\arguments{
\item{df}{A DataFrame containing the raw web portal data.}

\item{CSV_OUTPUT_DIR}{Directory to place the created csv file.}
}
\value{
NULL This function does not return any value. It writes the processed
data to a CSV file.
}
\description{
This function processes web portal data by capitalizing column
names, replacing spaces with underscores, replacing Excel-style dots with
underscores, and trimming datetime columns to remove the time component. The
processed data is then saved as a CSV file.
}
\details{
The function performs the following operations:
\itemize{
\item Capitalizes all column names in the data frame.
\item Replaces spaces with underscores in column names.
\item Replaces dots (.) in column names with underscores, handling Excel-specific
formatting.
\item Trims datetime columns to remove the time portion, leaving only the date.
}

The processed data is saved to a CSV file with the name \code{"web_portal.csv"}
in the directory defined by the global variable \code{CSV_OUTPUT_DIR}.
}
