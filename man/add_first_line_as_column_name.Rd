% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ToolsPdfExtraction.R
\name{add_first_line_as_column_name}
\alias{add_first_line_as_column_name}
\title{Add First Line as Column Name}
\usage{
add_first_line_as_column_name(tables, table_index)
}
\arguments{
\item{tables}{A list of character vectors, where each element represents a
table. Each table is a sequence of lines, and the first line of the specified
table is used to generate new column names.}

\item{table_index}{An integer specifying the index of the table in the
\code{tables} list. The function extracts and processes the first line of this
table.}
}
\value{
A character vector of updated column names:
\itemize{
\item The first element is the processed first line of the table (extracted
after the \code{"-PCR"} substring and with \code{ß} replaced by \code{"beta"}).
\item The remaining elements are the modified \code{COLUMN_NAMES}, where each column
name is updated by prepending the processed first line.
}
}
\description{
This function extracts the first line from a specified table in a list of
tables, processes it by extracting the substring after \code{"-PCR"} and
replacing any occurrence of \code{ß} with \code{"beta"}. The processed first line is
then added to the existing \code{COLUMN_NAMES}, modifying the subsequent column
names to incorporate the first line.
}
\examples{
tables <- list(c("Sample -PCR data", "Line 2", "Line 3"),
               c("Another -PCR example", "Line A", "Line B"))
COLUMN_NAMES <- c("Column1", "Column2", "Column3")
updated_columns <- add_first_line_as_column_name(tables, 1)
print(updated_columns)
}
